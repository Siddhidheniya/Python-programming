W= input('Please enter a string ')
def most_frequent(string):
    d = dict()
    for key in string:
        if key not in d:
            d[key] = 1
        else:
            d[key] += 1
    for x, y in d.items():
      print("{} = {}".format(x,y),end=" ")
    
most_frequent(W)
